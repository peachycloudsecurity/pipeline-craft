apiVersion: tekton.dev/v1beta1
kind: TaskRun
metadata:
  name: taskrun-maven-test
spec:
  taskRef:
    name: maven-test
  resources:
    inputs:
    - name: git-res
      resourceRef:
        name: git-resource
